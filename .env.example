# Agent OS Environment Configuration
# ===================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# The .gitignore file is already configured to exclude it.

# =============================================================================
# API Keys (Required for data collection)
# =============================================================================

# OpenAI API Key
# Get yours at: https://platform.openai.com/api-keys
# Required for: fetch_openai task
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic API Key
# Get yours at: https://console.anthropic.com/settings/keys
# Required for: fetch_anthropic task
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# =============================================================================
# Optional: Application-Specific Settings
# =============================================================================
# These are used by applications built on top of agent_os (e.g., billing tracker)

# Discord Webhook URL (for alerts)
# Create a webhook: Server Settings > Integrations > Webhooks > New Webhook
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN

# Alert threshold in dollars (triggers notification when exceeded)
# ALERT_THRESHOLD=50.0

# =============================================================================
# Optional: Agent OS Core Settings
# =============================================================================
# These override default paths for database and log files

# Database file path (default: agent_os.db in current directory)
# AGENT_OS_DB_PATH=/path/to/custom/agent_os.db

# Log file path (default: agent.log in current directory)
# AGENT_OS_LOG_FILE=/path/to/custom/agent.log
